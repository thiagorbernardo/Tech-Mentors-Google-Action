/*export default function setWordRequirements(word) {
    let final = "";
    let words = word.split(" ");
    for (let i = 0; i < words.length; i++) {
        final += words[i] + "_";
    }
    return final.slice(0, final.length - 1) ;
}*/